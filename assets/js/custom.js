document.addEventListener('DOMContentLoaded', function() {
    const mainMenu = document.getElementById('main-menu');
    if (mainMenu) {
        const currentItem = mainMenu.querySelector('.current');
        if (currentItem) {
            // Do nothing, prevent reordering
        }
    }
});